import { Brain } from "lucide-react";

import { StubPage } from "@/components/layout/stub-page";

export default function MemoriaPage() {
  return (
    <StubPage
      icon={Brain}
      title="Memória"
      description="Editor unificado de PROJECT_STATE, CHANGELOG, ADR e TECH_DEBT do projeto conectado."
    />
  );
}
