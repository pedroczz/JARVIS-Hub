import { Component } from "lucide-react";

import { StubPage } from "@/components/layout/stub-page";

export default function ComponentesPage() {
  return (
    <StubPage
      icon={Component}
      title="Componentes"
      description="Catálogo dos componentes de UI do projeto conectado, com uso e dependências."
    />
  );
}
